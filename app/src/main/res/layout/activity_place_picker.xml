<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/mappls_demo_background_colorSecondary"
    tools:context=".activity.widgets.PlacePickerActivity">
    <include
        android:id="@+id/top_header"
        layout="@layout/mappls_app_header" />

    <TextView
        android:id="@+id/selected_place"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="12dp"
        android:text="Please Select the Location"
        android:textColor="@color/mappls_demo_text_colorPrimary"
        android:padding="4dp"
        android:maxLines="1"
        android:ellipsize="end"
        android:gravity="center"
        app:layout_constraintBottom_toTopOf="@id/place_picker"
        />

    <TextView
        android:id="@+id/place_picker"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:text="Place Picker"
        android:background="@drawable/tranparent_btn_bg"
        android:padding="12dp"
        android:textColor="@color/mappls_demo_colorPrimary"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>

    <FrameLayout
        android:id="@+id/transit_fragment_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clickable="false"

        />
</androidx.constraintlayout.widget.ConstraintLayout>